<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Dark theme with Action Bar -->
    <style name="Base.Theme.MenuApp" parent="Theme.MaterialComponents.DayNight.DarkActionBar">
        <!-- Customize your dark theme here -->
        <item name="colorPrimary">@color/my_dark_primary</item>
        <item name="colorPrimaryVariant">@color/my_dark_primary_variant</item>
        <item name="colorOnPrimary">@color/white</item>
        <item name="colorSecondary">@color/my_dark_secondary</item>
        <item name="colorOnSecondary">@color/white</item>
        <item name="android:fontFamily">sans-serif</item>

        <!-- This will affect the default icon tint color -->
        <item name="colorControlNormal">@color/white</item> <!-- Default color for icons -->
        <item name="colorControlActivated">@color/white</item> <!-- Activated item color -->
        <item name="android:windowBackground">@color/backg</item>

        <!-- Keep the Action Bar -->
        <item name="windowActionBar">true</item>
        <item name="windowNoTitle">false</item>
    </style>

    <style name="Theme.MenuApp" parent="Base.Theme.MenuApp"/>
</resources>