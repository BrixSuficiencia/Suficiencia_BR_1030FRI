<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Light theme with Action Bar -->
    <style name="Base.Theme.MenuApp" parent="Theme.MaterialComponents.DayNight.DarkActionBar">
        <!-- Customize your light theme here -->
        <item name="colorPrimary">@color/my_light_primary</item>
        <item name="colorPrimaryVariant">@color/my_light_primary_variant</item>
        <item name="colorOnPrimary">@color/white</item>
        <item name="colorSecondary">@color/my_light_secondary</item>
        <item name="colorOnSecondary">@color/black</item>
        <item name="android:fontFamily">sans-serif</item> <!-- Built-in font -->

        <!-- This will affect the default icon tint color -->
        <item name="colorControlNormal">@color/black</item> <!-- Default color for icons -->
        <item name="colorControlActivated">@color/black</item> <!-- Activated item color -->
        <item name="android:windowBackground">@color/backg</item>

        <!-- Keep the Action Bar -->
        <item name="windowActionBar">true</item>
        <item name="windowNoTitle">false</item>
    </style>

    <style name="Theme.MenuApp" parent="Base.Theme.MenuApp"/>
</resources>